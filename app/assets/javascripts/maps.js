// Now, since there is not code that calls to the initMap function
//  (remember: who called it, it was the sdk script which call it only in the async mode),
//   you need to call it by yourself. So, just call it in the end of the controller.

